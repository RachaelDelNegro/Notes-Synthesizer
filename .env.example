# Backend
PORT=3001
NODE_ENV=development

# If you're using SQLite
DB_PATH=backend/data/notes_synthesizer.sqlite

# LLM provider (optional for now—can stay blank until you wire it up)
LLM_PROVIDER=

# App metadata (nice for logging)
PROMPT_VERSION=v0.1
MODEL_NAME=mock-model
